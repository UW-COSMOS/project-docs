#!/usr/bin/env node

const fs = require("fs");
const data = fs.readFileSync("/dev/stdin", "utf-8");

const result = data.replace(/<img src="(\S+)".+alt="(\S+)".+\/>/g, "\n![$2]($1)\n");

console.log(result);

